package com.gestionprojet.config;

/**
 * CORS configuration is now handled in SecurityConfig.
 * This class is kept for backward compatibility but is empty.
 * All CORS configuration is centralized in SecurityConfig for better security management.
 */
public class CorsConfig {
    // CORS configuration moved to SecurityConfig
}
