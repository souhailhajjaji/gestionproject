package com.gestionprojet.model.enums;

/**
 * Enumeration of user roles in the system.
 * ADMIN has full access to all features, USER has standard access.
 */
public enum Role {
    ADMIN,
    USER
}
